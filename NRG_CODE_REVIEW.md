# 数値繰り込み群 (NRG) コードレビュー

## 再レビュー依頼への対応状況

- ご指定の `anderson_nrg_cfs_ratio_0911.cpp` を対象に再レビューを実施するため、リポジトリ内ファイルを再走査しました。
- しかし、現時点の作業ツリーには同ファイルが存在せず、`.cpp/.h` 系の実装ファイルも確認できませんでした。

## 確認結果（事実ベース）

- `git status --short` は空で、未追跡ファイルや未コミット差分はありません。
- `find . -maxdepth 2 -type f | sort` の結果でも、実体ファイルは `README.md` と本レビュー文書のみでした（`.git` 配下を除く）。

## レビュー結論

現時点では **レビュー対象の NRG 実装ファイルを取得できていないため、コード行単位のレビューは実施不可** です。

## 受領後に即実施するレビュー観点

`anderson_nrg_cfs_ratio_0911.cpp` がリポジトリに反映され次第、以下を優先順でレビューします。

1. **ハミルトニアン構築の正当性**
   - Anderson impurity model の各項（impurity, bath, hybridization, Coulomb）が式通り実装されているか。
2. **NRG 反復更新の整合性**
   - Wilson chain の係数生成、反復対角化、再スケーリングが一貫しているか。
3. **状態切り捨てと精度管理**
   - `Nkeep`、エネルギーカット、縮退処理の方針が再現可能かつ妥当か。
4. **数値安定性**
   - 固有値計算での不安定化（桁落ち・不適切な閾値）を誘発する箇所がないか。
5. **物理量計算の妥当性**
   - CFS ratio 計算経路、規格化、温度依存の扱いが既知ベンチマークと整合するか。
6. **実装品質**
   - メモリ所有権、不要コピー、境界条件、例外時挙動、ログ/出力の追跡可能性。

## お願い（最短で再レビューするため）

- `anderson_nrg_cfs_ratio_0911.cpp` をこのブランチにコミット（または少なくとも作業ツリーに配置）してください。
- 併せて、使用ライブラリ（Eigen/Armadillo/LAPACK など）とビルド方法が分かる最小手順を共有いただけると、コンパイル確認込みでレビューできます。
